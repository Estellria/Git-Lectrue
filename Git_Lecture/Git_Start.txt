쉽게 설명하는 Git 기초 part 1.

git add 파일경로 -> staging area에 올린다.
git add .       -> 상위 경로에서 하위 경로의 파일들을 전부 올린다.




-- Git Hub 연결 --

git remote -v   -> 현재 깃헙이 연결 되어 있는지 확인하고 연결 되어 있다면 주소를 반환
git push        -> hub에 staging area에 있는 데이터를 hub에 올림.


git pull 주소   -> 
git clone 주소  -> 